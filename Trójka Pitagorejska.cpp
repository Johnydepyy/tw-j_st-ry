#include <iostream>
using namespace std;
int main ()
{
	int a,b,c;
	cin>>a>>b>>c;
	if(cyfry[i]*cyfry[i] + cyfry[i+1]*cyfry[i+1] == cyfry[i+2]*cyfry[i+2] or cyfry[i+2]*cyfry[i+2]-cyfry[i]*cyfry[i]==cyfry[i+1]*cyfry[i+1] or cyfry[i+2]*cyfry[i+2]-cyfry[i+1]*cyfry[i+1]==cyfry[i]*cyfry[i])
		
			cout<<"to jest trojka pitagorejska";
		}
		else
		{
			cout<<"to nie jest trojka pitagorejska";
		}
		return 0;
}